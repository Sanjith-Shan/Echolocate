# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.venv/
venv/
env/
*.db
*.sqlite

# Secrets
.env
*.pem
private_key*
public_key*

# Node / Vite
node_modules/
# NOTE: We use frontend/dist/ as a hand-written vanilla PWA, not a build
# artifact, so it IS checked in. If we ever switch back to Vite, re-add it.
*.log

# ESP-IDF build artifacts
firmware/build/
firmware/sdkconfig
firmware/sdkconfig.old
firmware/dependencies.lock
firmware/managed_components/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
